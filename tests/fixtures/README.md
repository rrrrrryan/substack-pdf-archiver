Put only **sanitized** fixture HTML in this directory.

Do not commit raw saved Substack HTML from a real logged-in account without removing preload blobs and checking for personal data first.

Synthetic fixtures are preferred when they can cover the regression you need.
